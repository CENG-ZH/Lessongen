# DOCX 输入兼容性验收（2026-09-15）

使用正式 `SafeDocxInspector` 与 `DocxExtractor` 检查 5 份结构不同的脱敏 Word，结果如下：

| 样例 | 结果 | 抽取概况 | 警告/失败语义 |
|---|---|---|---|
| 酸碱中和反应生成稿 | 通过 | 38 段、8 表、8,141 字符 | 0 条警告 |
| 有丝分裂生成稿 | 通过 | 45 段、9 表、10,235 字符 | 0 条警告 |
| 早期示例教案 | 通过 | 33 段、7 表、3,172 字符 | 1 条可见警告 |
| 导数与函数单调性生成稿 | 通过 | 43 段、6 表、6,581 字符 | 0 条警告 |
| 学科网英语样例 | 明确拒绝 | 未进入规范化或 Pipeline | `UNSAFE_DOCX`：包含外部关系 |

结论：4/5 样例可靠进入抽取；不安全样例明确失败，没有生成空壳教案。系统不承诺保留上传 Word 的原排版，输出统一使用项目的标准 DOCX 模板。
