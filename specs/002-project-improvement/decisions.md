# 决策与待确认事项

本页防止“看上去已规划”被误读成“所有取舍已获导师批准”。状态含义：`确认`＝已有明确项目约束；`建议`＝本规格默认方案，实施前复核；`待确认`＝涉及产品/研究/授权取舍，不能由编码自行决定。

| ID | 状态 | 决策或问题 | 原因与下一步 |
| --- | --- | --- | --- |
| D01 | 确认 | F `Lessongen` Git 仓库为唯一后续代码仓库；C 盘旧工程只保留参考。 | 用户已明确最新版本与 Git 位于 F；P0 要保证进程实际来源一致。旧目录暂不删。 |
| D02 | 确认 | 根 Compose 的 MySQL 3307 是完整产品唯一主线路；本机 MySQL 3306 仅保留为旧实例。 | 用户要求简化运行环境；一键脚本、README 和 CI 均按 3307 验收，旧 3306 不删除、不混用。 |
| D03 | 确认 | Python 与 Java 共同使用保留的 `F:\comalesson\runtime\lesoongen`，新安装则使用仓库相对目录。 | 已只读验证历史 JSON、Word、上传原件与引擎状态；不移动或改写旧文件。 |
| D04 | 建议，需项目负责人确认 | 生成默认至少一次三 Critic 独立审查；Judge-first fast path 仅为实验对照。 | 当前 8.275 分 v0 可直接结束，与 Paper#4“多角色审查”表达不一致。此改动增加时延与费用，需确认演示/研究优先级。 |
| D05 | 待确认 | 强制初审时若预算无法覆盖三 Critic，该 run 应标 `needs_human`、预算停止并交付 v0，还是直接 `failed`？ | 两者影响 Java 映射和 UI 文案；不得在实现中临时选择。建议保留 v0 但不得标 `quality_passed`。 |
| D06 | 待确认 | 三 Critic 一人失败时是否允许两人意见降级交付？ | 建议不能标“完成三方审查”；可保存部分账本和 v0，重试/人工复核由预算控制。 |
| D07 | 待确认 | RAG 中哪些课标/教材内容可存储、检索和展示？ | 权利、版本和适用范围需要导师/素材提供方核准；无授权时只能用用户明确提供的片段或公开授权材料。 |
| D08 | 待确认 | Paper#3 报告契约、模拟角色和后验证触发时机。 | 现有 adapter 只证明数据转换可能，不证明模拟可靠或已接入主图；需与 Paper#3 负责人对接。 |
| D09 | 待确认 | 人工评价的样本、评分者、伦理审批、数据保留期限与发布边界。 | “能发论文/可上线”不能从内部 Judge 分数推断；P3 需导师、学校和参与者的正式安排。 |
| D10 | 建议 | CD 先限于可复现打包和受控演示，不自动公开部署。 | 当前 Web MVP 无登录/权限；公开服务还需认证、配额、隔离、隐私和滥用防护规格。 |

确认方式：在实施对应任务前，由负责人在 PR/组会纪要中记录“决策 ID、选择、理由、日期”；然后同步修订 `spec.md`、`plan.md` 和测试。没有确认的 P2/P3 项可做离线概念验证，但不能宣传为产品能力。
